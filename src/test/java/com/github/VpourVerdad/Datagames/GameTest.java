package com.github.VpourVerdad.Datagames;

import com.github.VpourVerdad.Datagames.entities.Game;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

import static org.junit.jupiter.api.Assertions.*;

/*@SpringBootTest
public class GameTest {

    @Test
    void testName() {
        Game game = new Game();
        game.setName("My Game");
        assertEquals("My Game", game.getName());
    }

    *//*@Test
    void testPublisher() {
        Game game = new Game();
        game.setPublisher("My Publisher");
        assertEquals("My Publisher", game.getPublisher());
    }

    @Test
    void testPlayerCount() {
        Game game = new Game();
        game.setMinPlayers(2);
        game.setMaxPlayers(4);
        assertEquals(2, game.getMinPlayers());
        assertEquals(4, game.getMaxPlayers());
    }

    @Test
    void testGenre() {
        Game game = new Game();
        game.setGenre("Strategy");
        assertEquals("Strategy", game.getGenre());
    }

    @Test
    void testYear() {
        Game game = new Game();
        game.setYear(2021);
        assertEquals(2021, game.getYear());
    }

    @Test
    void testAuthor() {
        Game game = new Game();
        game.setAuthor("John Doe");
        assertEquals("John Doe", game.getAuthor());
    }

    @Test
    void testDuration() {
        Game game = new Game();
        game.setDuration(60);
        assertEquals(60, game.getDuration());
    }*//*

    @Test
    void testFavorite() {
        Game game = new Game();
        game.setFavorite(true);
        assertTrue(game.isFavorite());
    }

    @Test
    void testNameNotEmpty() {
        Game game = new Game();
        assertThrows(IllegalArgumentException.class, () -> game.setName(""));
        assertThrows(IllegalArgumentException.class, () -> game.setName(null));
    }


}*/
